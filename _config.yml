title: Endpoint Security
email: info@endpoint-sec.com
description: We deliver air-gap security for remote endpoints by ensuring that data is only decrypted at the user’s eye level.
baseurl: ""
domain: "endpoint-sec.com"
url: "https://www.endpoint-sec.com/"

theme: just-the-docs
logo: assets/logo.png
nav_enabled: false
search_enabled: false
heading_anchors: false

exclude:
  - README.md
  - serve
  - "*.code-workspace"

sass:
  quiet_deps: true
  silence_deprecations: ['import']
